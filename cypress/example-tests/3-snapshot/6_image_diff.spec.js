const routes = ['https://www.saucedemo.com/inventory.html', 'https://picsum.photos/'];

describe('Component screenshot', () => {

    /*it('should display the index page correctly', () => {
      cy.visit('https://picsum.photos/')
      cy.wait(4000)
      cy.compareSnapshot('index', {
        capture: 'fullPage',
        errorThreshold: 0.1
      });
    });

    it('should display the component correctly', () => {
      if (Cypress.env('type') === 'base') {
        cy.visit('https://picsum.photos/')
        cy.wait(4000)
        cy.get('h1').contains('Lorem Picsum');
        cy.get('h1').contains('Lorem Picsum').compareSnapshot('Lorem Picsum');
      } else {
        cy.visit('https://picsum.photos/')
        cy.wait(4000)
        cy.get('h1').contains('Lorem Picsum').compareSnapshot('Lorem Picsum').should('be.true');
        cy.get('h1').contains('Lorem Picsum').compareSnapshot('Lorem Picsum', 0.02).should('be.true');
      }
    });

    it('should display the component correctly', () => {
        cy.visit('https://picsum.photos/')
        cy.wait(4000)
        cy.get('h1').contains('Lorem Picsum').compareSnapshot('Lorem Picsum')
    });

    after(() => {
      cy.task('generateReport')
    }

    it('Loads the homepage', function() {
      // Load the page or perform any other interactions with the app.
      cy.visit('https://picsum.photos/')
      cy.wait(4000)
  
      // Take a snapshot for visual diffing
      cy.percySnapshot();
    });
    */
})

